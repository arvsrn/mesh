export interface Blob {
    position: number[];
    color: string;
    blending: string;
    radius: number;
}

export interface Text {
    position: number[];
    text: string;
    blending: string;
    color: string;
}
